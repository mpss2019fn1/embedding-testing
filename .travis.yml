dist: xenial
language: python
python:
  - "3.7"
install:
  - pip install -r requirements.txt
script: "coverage run -m pytest --cov-report term --cov=$TRAVIS_BUILD_DIR/src/"
after_success:
  - coveralls