class TaskCategory:

    def __init__(self, name, enabled, tasks, categories):
        self._name = name
        self._enabled = enabled
        self._tasks = tasks
        self._categories = categories
